# 交接文档（HANDOFF）

> 本文件写给**没有任何对话历史、只有这个仓库**的下一位执行方（如 opencode 里跑 DeepSeek）。
> 所有靠对话累积的约定、教训、判断依据，都写在这里 —— 不写进去就等于没有。
> 开始任何工作前，请先完整读完本文件与仓库根目录的 `README.md`、`CHANGELOG.md`。
> 如发现本文件与仓库现状不符（数值、路径、测试数），先停下向组里核实，不要自行假设。

---

## ① 当前状态快照（2026-08-06 编写时）

| 项 | 值 |
|---|---|
| 当前分支 | `dev` |
| 最新 commit | `a89371c`（3b-2：hfs 补 CSV/JSON 导出 + 循环徽标 + 说明搬迁 + Steck 交叉向量） |
| 工作区 | **干净**（`git status --short` 无输出） |
| 相对 origin | 领先 9 个 commit（未 push 是正常，本地日常在 dev 上推进） |

### 五套测试的准确通过数（本地逐一跑过验证）

| 套件 | 命令 | 结果 |
|---|---|---|
| bench.js | `node tests/bench.js` | **40 / 40** 全部通过 |
| bench.py | `python3 tests/bench.py` | **40 / 40** 全部通过（另附全库校验：18 条跃迁 lam/ek 一致、d 有限为正） |
| crosscheck | `node tests/crosscheck.js` | **全部一致**（7 项常数两侧逐位一致） |
| smoke | `node tests/smoke.js` | **18 / 18** 烟雾测试全部通过 |
| browser | `node tests/browser.js` | **59 项，失败 0**，无 console error（剪贴板断言已移出计数——headless 下异步剪贴板时好时坏，曾致 59/60 波动，见 CHANGELOG） |

四条一键命令（即 `npm test`）：`node tests/bench.js && python3 tests/bench.py && node tests/crosscheck.js && node tests/smoke.js`；浏览器单独 `node tests/browser.js`。
本地开发先 `python3 -m http.server 8000` 再开 `http://localhost:8000`。浏览器测试若本地 8000 未起，会自动临时起一个。

**测试向量组（`tests/vectors.json`）**：constants 2 / single_photon 6 / hyperfine 5 / raman 8 / wigner 10 / dipole_convention 4 / applicability 2 / **steck_cross 3**。

### 四个数值锚点（改架构、改公式、改常数后必须纹丝不动）

| 锚点 | 值 | 含义 |
|---|---|---|
| `d_red` | **0.540659** e·a₀ | ⟨¹S₀‖er‖³P₁⟩ 约化矩阵元，Edmonds/Steck 归一化，由 Γ=183 kHz 反解 |
| `d_cyc` | **0.312150** e·a₀ | 每个球张量分量 q 的裸矩阵元 = d_red/√3（J=0 基态时才成立） |
| `λ` | **555.802363** nm | 真空波长，由 NIST 能级 E_k=17992.007 cm⁻¹ 经 `10⁷/E_k` 导出 |
| `Ω_R/2π` | **1.7261** MHz | 默认工作点的拉曼 Rabi 频率（默认圆光斑、d_red、默认 P/w） |

`tests/smoke.js` 已锁定这四个锚点；`tests/browser.js` 也有对应断言。四锚点任何变动都是严重信号。

### 部署情况（Cloudflare Pages，main/dev 双车道）

- **`main`** → 生产 `https://yb-toolkit.pages.dev`，右上角 `stable`，组里其他人用。
- **`dev`** → 每次 push 到 dev 自动生成独立预览网址（见 Pages → Deployments 里对应构建链接），右上角显示 commit hash + 「开发版」徽标。
- **`scripts/stamp.sh`**：构建命令。用 `git rev-parse --short HEAD` 取 commit、`date` 取日期。**关键：Cloudflare Pages 构建时用 detached HEAD 检出，`git rev-parse --abbrev-ref HEAD` 拿不到真实分支名，必须优先用环境变量 `CF_PAGES_BRANCH`**（Pages 提供，准确反映当前构建分支），否则会把生产分支误标成 dev。`BRANCH="${CF_PAGES_BRANCH:-$(git ...)}"`，非 main → `CHANNEL=dev`。注入到 `shared/version.js` 的 `__COMMIT__`/`__DATE__`/`__CHANNEL__` 占位符。
- CI（`.github/workflows/test.yml`）：`vectors` job（四套物理/挂载测试）每次 push 到 main/dev 都跑；`browser` job 只在 main push 或 PR 跑（Playwright 装浏览器重且慢，避免拖累 dev 反馈速度）。

---

## ② 六条硬规则（逐字 + 来历）

> 规则没有来历就只是教条，接手方会绕过它。以下每条都附「怎么来的」。

**⓪ 绝不存文献 d 值，一律由 Γ 反解。**
偶极矩阵元有多套归一化（`d_Edmonds = √(2J+1)·d_CG`），混用是本项目栽跟头最多的地方。J=0 基态时该因子恰为 1，所以 Yb 一直没暴露问题，换到碱金属（J=1/2）就会差 √2。由 Γ 反解可保证约定自洽。
*来历：hfs 计算器 v2.1 的教训 —— 在 Edmonds vs CG 归一化上**两次判断失误**（先错误地把 0.543 除以 √3，后又错误地否认 √2 因子存在；查 Steck 原表才确认该因子真实且 J=0 时恰为 1）。加入自检的直接动因就是"约定问题上曾两次判断失误，口头保证不如让使用者在浏览器里现场复核"。*

**① 常数只在 `shared/constants.js` 定义一次。**
工具页面里出现任何裸数字都是 bug 的温床。我们已经因为"同一常数各写一份"栽过三次：d 的两种归一化混用、Δ_hf 符号取反、α 被硬编码为 45°。每个常数必须带 `src` 说明它从哪来。
*来历：v1 多文件各写一份导致三处物理错误，全是"同一份东西存两处"的下游症状。*

**② 公式只在 `shared/physics.js` 定义一次，改动必须先过测试向量。**
`tests/vectors.json` 里的期望值全部来自 notebook 输出或论文原文，不是"当前代码算出来的值"。这是它有意义的前提。
*来历：编写「3j 半整数」向量时期望值凭记忆填写并出错，由 sympy 核实后修正 —— 证明向量期望值必须来自权威计算或文献原文，不能来自记忆或当前代码输出。*

**③ 不在绘图循环里调 `getComputedStyle`、读 `clientWidth`、或解析单位。**
读布局与写 DOM 交替会触发同步重排，页面在连续输入时会卡死。`shared/plot.js` 已把主题色和画布尺寸缓存好，用它提供的接口即可；单位在每次重绘开始时解析一次并缓存，不在循环内反复调 `YBU.resolve`。
*来历：v7 前每帧约 100 次 getComputedStyle + 3 次 clientWidth 强制同步重排，中间夹 innerHTML 重建，连续拖滑块卡死。缓存后降到 2.1 ms/次按键。*

**④ Store 只存输入，派生量一律现算。**
存 `transition`（元素/同位素/能级/Γ），不存 `d`、`λ`、`CG`。缓存派生量会产生"改了同位素但 d 没跟着变"这类静默错误 —— 本项目最危险的一类 bug。
*来历：模块间联动时若缓存派生量，下游不会跟随上游改动，且无任何报错。*

**⑤ 不用脚本批量改写源码或数据文件。**
用精确的小步编辑，每步之后立即 `node --check`（JS）或 `json.load`（JSON）验证。需要临时构造坏数据做反向验证时，在内存里构造，不写磁盘。
*来历：违反此条**两次损坏文件** —— 一次 `json.dump` 改写 `data/transitions.json` 意外损坏（一度只剩 `{`，且 json.dump 会重排键序、中文转 \uXXXX、丢缩进）；一次用 Python 批量删波片代码块误删了 `compute()` 与 `V.ram`，模块语法错误、页面直接起不来。两次都是"用脚本大范围改写，改完才发现坏"。*

---

## ③ 已踩过的坑（按模式归类）

> 同一模式重复出现过的已标出次数。这些都是真实发生过、会导致返工或损坏的坑。

**A. 「假设某状态已就绪但实际未必」—— 出现过四次**
- v-d 三处：独立页 `tools/raman-qubit.html` 的 `pick()` 里 `document.getElementById('v-d')` 但页面没有该元素（字段命名不同），且无 null 保护，每次加载都抛异常。该页**三处**同类访问（style、num 读 value、calc 写 value）+ `num()` 助手本身，逐一补 null 保护。这导致独立页 `S.d = num('v-d')*AU = 0`，所有耦合理论值恒为 0 —— 是 3b 薄壳前"独立页 vs 模块理论值差异"的根因。
- hfs 模块异步就绪竞态：`cur={el:null}` 要等 `loadDB()` Promise 兑现才填值，但 `render()` 里 `var d=DB[cur.el]` → `DB[null]` 是 undefined → `d.tr` 抛错。触发：DB 已加载但 cur 未就绪的时间窗内展开折叠区；以及 file:// 下 loadDB() reject 后任何 toggle 必抛。修复：render() 开头 `if (!DB || cur.el===null || cur.tr===null) return;`。这是**同一模式第四处**（前三处是独立页 v-d 三处访问）。
- 独立页异步化后，`compute()/loadDB()/buildTr()/buildIso()/自检 $("run")` 全部加 `if(!ready())` 守卫。

**B. 「用脚本批量改写文件」—— 出现两次，两次都损坏文件**
- `json.dump` 改写 `data/transitions.json` 损坏（只剩 `{`）。
- Python 批量删波片代码块误删 `compute()` 与 `V.ram`，模块起不来。
- 教训：一律用精确小步 Edit + 每步 `node --check`；临时坏数据在内存里用 `copy.deepcopy` 构造（`validate_transitions()` 已重构为接受 `db` 参数），不碰磁盘。见硬规则⑤。

**C. 「测试绿灯但没测到东西」—— 出现两次**
- J≠0 时画布清空断言**写反**：无效态下图表被 `prep()` 清空（0 不透明像素）是设计意图（不显示旧数值），不是空屏 bug，断言最初误判为失败。
- 紧凑模式测试用 Yb 测不出截断：Yb 塞曼表本就只有 6 行，30→6 截断无体现，会"让坏的功能看起来是好的"；换 Sr（30 行）才验证到。测试用例选数据要让目标差异足够大。

**D. 物理闭合的假象（见 ④）**
- K1 单位 bug：见下节，是最重要的一例。

**E. 其他记下防重复的**
- 折叠区不会自动重新展开：`setCompact(false)` 只收起、不展开，恢复高度后 `<details>` 需手动点开。是有意设计，断言只锁行数恢复，不锁自动展开。
- `render()` 竞态修复的「DB 已加载但 cur 未就绪」半段无测试保护：烟雾环境 fetch 被 reject、DB 恒 null，走不到那半段，唯一覆盖是 browser.js（真实 HTTP 加载下有该窗口），目前未写专门断言。**已知测试边界，记下而非假装覆盖。**
- 浏览器测试的 53→54 计数（见文末「附录」）。
- 独立页 `tools/raman-qubit.html` 的画布代码尚未迁到 `shared/plot.js`，仍有 v7 之前布局抖动（README 待办 #3）；`modules/raman-qubit.js` 已用新写法。

---

## ④ 验证标准（具体到可执行）

**图表验证必须数不透明像素，不能只看无 console error。**
浏览器里画布画没画出东西，用 `getImageData` 数 alpha≠0 的像素数，而非只看没有报错。已有例（browser.js 里模式）：
```js
const d = ctx.getImageData(0,0,cv.width,cv.height).data;
let n=0; for (let i=3;i<d.length;i+=4) if (d[i]!==0) n++;
// n>0 → 画出内容；n===0 → 空屏（可能是设计清空，也可能是 bug，按 ③C 判断方向）
```
空屏可能是设计意图（无效态清空），也可能是 bug —— 先确认哪边才是正确行为再下结论。

**带计算的功能必须验到物理闭合。**
移植带计算的功能（拟合、反推、扫描）不能只看"图画出来、没报错"。要算出一个能交叉验证的物理量，看它是否闭合到已知值。**K1 单位 bug 是最重要的一例**：
- 3a-2b 移植波片/功率拟合时，模块功率扫描幅度比一度输出 **993**（荒谬）——如果只看"图出来了、没报错"会一路蒙混过关。
- 是**反推等效束腰 366.3µm ≈ 设定 365µm** 这个物理闭合把它逼出来的。修好后幅度比变成 **0.993**（≈1，正确）。
- 即验证标准：**幅度比 ≈ 1、反推束腰 ≈ 设定值** 才叫通过。此后移植带计算功能一律照此标准。

**测试变红时不许放宽容差或改 vectors.json 期望值，必须停下报告。**
向量期望值来自 notebook 或论文原文，不是当前代码的输出。测试红了，要么代码错、要么期望值录入错 —— 两种情况都要查根因并报告，而不是调大 `rtol`/`atol` 或偷偷改期望值让它绿。这是 `tests/vectors.json` 有意义的前提（硬规则②）。

**双实现必须同时通过。** `bench.js`（JS）与 `bench.py`（Python）读同一份 `vectors.json`，任何新 `fn` 必须在**两边**都加 IMPL 实现，否则一边会报「未实现」。新公式要两套都过再接界面（README「加一个新模块」步骤 5）。

---

## ⑤ 待办与决策记录（当前进度：3b-2 已完成）

### 时间线（最新在前）
- **3b-2**（`a89371c`）：hfs 补 CSV/JSON 导出 + 循环徽标 + 说明搬迁 + Steck 交叉向量。**已完成。**
- 之前：3a-1（rabi 三维曲面）、3a-2a（raman 设计图）、3a-2b（raman 波片+拟合）、3a-3（hfs 求和扫描+LaTeX）、3b-1（rabi 薄壳 + 椭圆光斑 + η·d）。

### 3b-2 已做的裁决（接手方需知，避免重做或误解）

**功能补三项，舍四项，每项附理由（这是已确认的裁决，直接执行后续即可）：**

*补：*
- **CSV 导出** —— 数据出口，别人拿数据去算没有导出就得手抄。
- **JSON 全库导出** —— 同上，可作下一版数据源（数据与代码分离）。
- **循环跃迁徽标** —— D1 线无伸展态故无循环跃迁，是选跃迁时的关键判据，藏起来会让人选错。

*舍（接受丢失，去掉是修正而非功能损失）：*
- **Γ/d 主控切换** —— "以 d 为准"就是规则⓪的漏洞，去掉是修正。
- **筛选器（F/F′/偏振/循环）** —— 塞曼表最多几十行，滚动比筛选快。
- **悬浮解释（32 处）** —— 量太大且与折叠区说明重复；折叠区能放更多字且可打印。
- **条目备注** —— 内存存储、刷新即丢，是假功能。真要记备注应进 `data/`，那是另一件事。

**说明搬迁方案（已执行）：**
- exp2「数据出处与约定」扩成三段：归一化陷阱 / Edmonds vs CG（含 Steck 4.227 例）/ 纪律。
- 新增 exp5「更新记录」。**更新记录要重写成模块自己的历史，不是照搬独立页工具史**；保留有长期价值的部分，尤其「约定两次判断失误」那段（那是自检存在的理由、规则⓪的来历）；纯 UI 改动可删。
- **循环跃迁说明与徽标放同一处**，不要分到两处，说明要紧贴徽标。

**Steck 交叉验证（已执行）：**
- 新建 `steck_cross` 向量组，3 条：Rb D2 约化矩阵元（CG 归一化）4.227、伸展循环跃迁 2.989、空气波长 780.0320。
- **期望值必须来自 Steck 原表，不是从本代码算出来的。**拿不到原始数值，就在向量 `src` 里写明来源与不确定度。这是规则②的底线。
- 设计判断：这 3 条**跨归一化约定**，是求和扫描测不到的层 —— 求和扫描测同一套约定内部的自洽，永远测不到"约定对不对"。⑪ 项自检里只有 Steck 交叉验证是真独有的，其余被全库扫描覆盖。

### 后续待办
- **3b-3：raman-qubit / hfs 改薄壳**。照 3b-1 对 rabi 的做法（`tools/rabi-power.html` 已改成 87 行薄壳，`YBM.mount('rabi-power', host, {compact:false})` + 一套 URL 恢复/写入），把 `tools/raman-qubit.html`、`tools/hfs-matrix-element.html` 也改成只挂载单模块的薄壳，消除界面代码重复。**注意**：模块已覆盖大部分功能（raman 模块已有 exp3 设计图、exp4 波片扫描+实测拟合；hfs 模块已有求和扫描、LaTeX、CSV/JSON、循环徽标），独立页仍多出的主要是 UI 类（悬浮解释、raman 的测量不确定度滑块 r-sw/r-sp/r-sd/r-si、Γ/d 切换等）—— 薄壳前按 3b-2 那套"补三项/舍四项+理由"的裁决框架核对哪些要补、哪些接受丢失。改薄壳后**独立页 vs 模块的 v-d 理论值差异会自动消失**（见 ③A）—— 薄壳后独立页读 Store 真实值。
- **README/CHANGELOG 收尾**：README「待办」条目逐条核对更新（其中 #2 模块与独立页收敛即 3b-3；#3 raman 画布迁移；#5 真实浏览器验证已完成要勾掉）。

### 长期待办（README「待办」）
1. `data/calibrations.json` 全为 null，待填实测值。偏振与几何标定（波片零点 θ₀、光束–磁场夹角 Θ_kB）完成前，拉曼模块的绝对幅度不应作定量预测。
2. 模块与独立页面收敛（3b-3）。
3. raman 独立页画布迁移到 `shared/plot.js`。
4. Γ 长期挂着 183/182 两个值，查一次原始文献定死一个。
5. CI 复用 `scripts/verify.sh`（**缓**）：当前 CI 拆 vectors/browser 两 job 是有意的——浏览器测试只在 main/PR 跑，省 dev 的反馈速度。verify.sh 一把梭会抹掉这个优化。若要统一，需让 verify.sh 支持"是否含浏览器测试"的开关。
6. 多人并行（**不适用**，当前单一执行方）：HANDOFF 与 PROGRESS 假定同一执行方顺序推进。若将来多人并行，需重新约定任务归属与 PROGRESS 尾部"当前任务"字段，避免冲突。

---

## ⑥ 工作协议

- **一个任务一次 commit，中文 commit 信息。** 每个 commit 说明改了什么与为什么（可含约束、验证结果）。参考仓库现有 commit 风格。
- **每个任务做完停下报告，等确认再继续。** 不要一口气做多个任务。3a 系列曾因对话截断绕过"做完停下等确认"的约定，事后才发现，务必遵守。
- **做较大改动（新增图、新增模块功能、搬说明）前，先出清单/方案让组里确认，再动代码。** 参考 3b-2 的做法：先列功能对照清单 + 方案，等裁决。

### 必须先问组里的事项（不要自作主张）
- **改 `tests/vectors.json` 里的期望值**（哪怕是"修正"）。期望值来自 notebook 或论文原文，动了要说明新值的来源。
- **改物理常数**（`shared/constants.js` / `shared/physics.js` / `shared/wigner.js` 的任何数值或公式）。
- **引入构建工具**（当前无构建工具，双击即可运行，这是有意的取舍）。
- **给 Store 加派生量缓存**（规则④明令禁止缓存派生量）。
- **填写 `data/calibrations.json`**（需要实测数据，不是推断值）。

---

## ⑦ 给接手方的一句直白提示

**这个项目此前的质量依赖执行方主动报告自己的失误。请保持同样的坦白 —— 报告一个自己造成的问题，比悄悄修掉有价值得多，因为同类问题往往不止一处。**

例证：每一次"悄悄修掉"的失误，最终都在别处暴露了同类问题（json.dump 损坏、批量删代码损坏、v-d 三处同类访问）。三处同类 bug 中，只修了第一处、没报其余两处，才会让它们一直潜伏到真实浏览器实测才暴露。**当你发现自己搞错了、破坏了什么、或者某个验证其实没覆盖到，写进 CHANGELOG / 报告给组里 —— 这比让测试看起来全绿重要得多。** CHANGELOG.md 的格式是"改了什么 + 为什么"，连自己的失误也要记（格式里有「对账」「待定」两类就是为此准备的）。

---

## 附录：浏览器断言 53 → 54 的来历（悬案结论）

接手方若看到历史记录里提到"浏览器断言从 53 变成 54"却查不出是加了哪条，答案在此：

**是计数方法差异，不是隐藏的新断言。** 两个数字都真实：
- `grep -c "ok(" tests/browser.js` = **53**，数的是**源码行**，其中包含：
  - 函数定义行 `function ok(name)`（含字面 `ok(` 但**从不作为断言执行**）；
  - 三工具页循环（`for (const t of ['rabi-power','raman-qubit','hfs-matrix-element'])`）里那 1 行 `ok(...)` 源行，运行时循环执行 3 次。
- 运行时 `PASSED.length` = **54**，数的是**实际执行的 `ok()` 调用**：`53 − 1（定义行不执行）+ 2（1 行循环 → 3 次执行） = 54`。

所以多出来的那条是循环体产生的第 2、3 条（`tools/*.html` 三个独立页各开一遍无 console error）。无异常，无遗漏断言。以后若想核对总数，以运行时输出（「通过 N 项」）为准，不要用 `grep -c`。
