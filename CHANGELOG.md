# 修订记录

格式：`物理 / 界面 / 性能 / 对账 / 待定`，每条写清**改了什么**与**为什么改**。
半年后回头看，"为什么"通常比"什么"更有价值。

## [0.3.1] — 2026-08-03 — 自查修复

### 物理
- **修复 J≠0 基态时静默返回 NaN。** 在 hfs 模块选中 Rb 等碱金属（基态 J=1/2）后，
  `d_cyc` 变为 NaN，rabi-power 模块整屏显示 NaN 且无任何提示。
  物理上返回 NaN 是对的 —— `d_cyc = d_red/√(2J′+1)` 的简化只在 J=0 基态成立，
  碱金属基态有两个 F，6j 因子不再被吸收 —— 但界面必须说明原因。
  新增 `derived.dCycValid()`，不适用时读数显示"—"并给出完整解释。
  已加入测试向量防回归。

### 界面
- 补齐提示条 LABEL 表的 12 个缺失路径（`beam.theta_kB_deg`、`limits.*` 等）。
  此前这些量改动时提示条显示原始路径名，违背"必须报出具体量"的设计意图。

### 已知问题（待 Claude Code 处理）
- **元素数据库有两份**：`data/transitions.json` 与 `tools/hfs-matrix-element.html`
  内联的 `const DB=[...]`。目前内容一致，但这正是本项目反复强调要消灭的模式。
  独立页面需改为 fetch 加载同一份 JSON。

## [0.3.0] — 2026-08-03 — 单页外壳 + 模块化 + 全局 Store

### 架构
- **从多页站点改为单页外壳 + 上下分栏。** 两个窗格可各选一个模块同时显示，
  中间分隔条可拖拽（双击复位）。窗格净高低于 420 px 时模块自动进入紧凑模式：
  收起折叠区、缩小图高、塞曼分量表只显示前 6 行。
- 新增 `shell/store.js`：全局状态。**硬规则是只存输入、不存派生量** ——
  存 `transition`（元素/同位素/能级/Γ），`d`、`λ`、`CG`、`Ω` 全部现算。
  缓存派生量会产生"改了同位素但 d 没跟着变"这类静默错误。
- 新增 `shell/registry.js`：模块注册与挂载。每个挂载实例分配唯一 id 前缀 ——
  rabi-power 与 raman-qubit 都用了 `v-p`/`v-d`/`v-f`，且同一模块可能同时挂在
  两个窗格，不隔离必然冲突。
- 新增 `shell/panes.js`：分栏、拖拽、紧凑模式、URL 编码（含窗格布局与分割比例）。
- 三个工具改写为 `modules/*.js`。模块只与 Store 对话，彼此不直接依赖 ——
  加第四、第五个模块时不会退化成一张依赖网。

### 联动
- 模块间自动联动，**外部改动会弹出提示条，并报出具体哪个量从多少变到多少**。
  只说"跃迁已改变"没有意义 —— 使用者需要能立刻判断影响。
  典型场景：hfs 模块里选中 Yb 556，其数据库 Γ = 182 kHz 会覆盖默认的 183 kHz，
  拉曼模块的 Ω_R 随之从 1.7261 降到 1.7166 MHz（−0.55%）。提示条会写明这一点，
  并提供撤销。
- `reads` / `writes` 声明区分"自己改的"与"别人改的"，前者不弹提示 ——
  否则拖个滑块就会弹一条。

### 性能
- 单位解析移出绘图热循环。此前 `uP()` / `uF()` / `uW()` 在每帧的曲线循环里
  被调用上千次，每次都做一遍查表。现改为每次重绘开始时解析一次并缓存，
  与 v7 的主题色缓存同理。`YBU.resolve` 对非法单位退回默认而非抛异常
  （畸形 URL 不应让整个模块崩掉），并对同一非法值只警告一次。

### 测试
- 新增 `tests/smoke.js`：无浏览器的挂载烟雾测试，16 项。校验三个模块能注册、
  能挂载、同一模块挂两次 id 前缀不冲突、Store 联动生效、派生量确实重算、
  撤销能恢复。**并锁定四个数值锚点**（d_red、d_cyc、λ、Ω_R），
  确保改架构不改变物理。已接入 CI。
- 物理向量仍为 35/35，JS 与 Python 双通道全通过。

### 保留的重复
- `tools/*.html` 三个独立页面保留，作为完整功能版本，主页面窗格标题栏有
  "完整版 ↗" 链接。模块目前只覆盖核心视图，尚未包含三维曲面、波片扫描拟合、
  42 组合自检等。**物理不会分叉**（两者共用 `shared/`），暂时重复的只有界面代码。
  收敛计划见 README「待办」。

## [0.2.0] — 2026-08-03 — 并入超精细矩阵元计算器

### 物理修正
- **波长改用 NIST 能级导出。** `λ_556` 由硬编码 556.0 nm 改为
  `10⁷/E_k`（E_k = 17992.007 cm⁻¹）= **555.802363 nm**。这从根上避免
  空气/真空波长混用。原值偏高 0.036%，使 d 偏高 0.053%。
  值得记的是：notebook cell 3 由 rydcalc 独立算出的正是同一波长，
  证据一直都在，只是此前没被采纳。
- **线宽保留两个值。** 默认 Γ/2π = 183 kHz（notebook 标定所用），
  备选 182 kHz（hfs 计算器所用），并记录文献分散 182–184 kHz。
  d ∝ √Γ，两者差 0.27%，均在实验不确定度内。
- 由上述两项，`d_cyc` 0.312316 → **0.312150**，`d_red` 0.540947 → **0.540659**。
  下游 Rabi 频率随之改变约 0.05%。

### 架构
- 新增 `shared/wigner.js`：Wigner 3j/6j（Racah 公式 + 对数阶乘），
  与 sympy 吻合到 1e-16，三角不等式不满足时返回 0 而非抛异常。
- **CG 表由硬编码改为 Wigner 计算。** 计算值与原硬编码值逐位相同，
  包括 F′=1/2 分支的负号 —— 该负号此前仅由推导得出，现由独立路径确认，
  它正是拉曼相消干涉因子 `Δ_hf/(Δ+Δ_hf)` 的来源。
- 新增工具 `tools/hfs-matrix-element.html`，删除其页面内重复的 Wigner
  实现改为引用共享模块（50.4 → 42.5 KB）。
- 测试向量 21 → 35 项，新增 `wigner` 与 `dipole_convention` 两组。
  其中「d_red = √3·d_cyc 两条路径一致」直接锁死了归一化约定。

### 采纳的纪律
- 数据库不存任何文献 d 值，一律由 Γ 反解。该原则来自 hfs 计算器
  v2.1 的教训（约定问题上曾两次判断失误），已写入 README 置顶规则。

### 备注
- 编写本次测试向量时，「3j 半整数」一项的期望值最初凭记忆填写并出错，
  由 sympy 核实后修正为 √30/15。这恰好说明向量的期望值必须来自
  权威计算或文献原文，不能来自记忆或当前代码的输出。

## [0.1.0] — 2026-08-03 — 框架搭建

### 架构
- 从两个独立 HTML 文件整合为多页站点，共享模块化。不引入构建工具：
  双击即可打开、断网可用、任何人能改一行 HTML。用 `<script src>` 而非
  ES module，因为后者在 `file://` 下会被 CORS 拦截。
- `shared/constants.js` 成为物理常数唯一来源，每项带 `src` 出处。
  **这是本次最重要的改动** —— 历史上 d 的两种归一化、Δ_hf 符号、
  α 被硬编码为 45°，全部源于同一常数在不同文件各写一份。
- `shared/physics.js` 成为公式唯一来源；`shared/plot.js` 沉淀 v7 的
  性能修复（主题色缓存、画布尺寸缓存、离屏复用、调度器）。
- `tests/vectors.json` 由 `bench.js` 与 `bench.py` 共同读取，
  两套实现必须同时通过；`crosscheck.js` 校验两侧常数逐位一致。

### 新增
- URL 状态编码：任一次计算都能变成可分享、可复现、可存档的链接。
- 版本戳：页面角落与 CSV 注释均带 `版本 · 日期 · commit`，
  排错时不必再猜对方是不是缓存了旧版。
- 控制台 API `window.YB`，按 F12 即可做零散计算。
- `data/` 目录纳入版本管理，标定值与文献值分离且可追溯。

### 已知待办
- `tools/raman-qubit.html` 的画布代码尚未迁移到 `shared/plot.js`，
  因此仍存在 v7 之前的布局抖动问题（连续拖动滑块会卡）。
- 两个工具的单位下拉尚未统一走 `shared/units.js` 的 `bind()`。
- `data/calibrations.json` 全为 null，待实验组填入。
  **在偏振与几何标定完成前，拉曼工具的绝对幅度不应作定量预测。**

---

以下为整合前各工具的历史，完整内容见对应页面内的「修订记录」区块。

## rabi-power（原 yb_rabi_power v5–v7）
- **v7** 消除输入卡顿。根因是布局抖动而非计算量：每帧约 100 次
  `getComputedStyle` 与 3 次 `clientWidth` 强制同步重排，中间又夹着
  `innerHTML` 重建。改为缓存 + 批量写入后降至 2.1 ms/次按键。
- **v6** 坐标裁剪到实验可达范围；单位自选；公式改用可分离形式
  `f_R = C√P/√(w_x w_y)`（数值逐位一致，相对偏差 < 3e-16）。
- **v5** 精简为纯矩阵元驱动，逐点复现 notebook 输出。

## raman-qubit（原 yb171_raman_calculator v1–v4）
- **v4** 补入微分光频移 `Δ_LS = δ_Z(8Ω_σ²+Ω_π²)/(4Δ²)`，工作点给
  55.6 kHz（论文 55.5 kHz）。此前只报散射误差，总门误差低估约 3.7 倍。
  修正 S₃ 模式下 α 被硬编码为 45° 的缺陷。
- **v3** 几何因子改写为 `|S₃|·sinΘ_kB`；新增 λ/4 波片模型 `S₃ = sin2(θ−θ₀)`。
- **v2** φ 改为 φ_HV 且 cos → sin（v1 默认工作点实际耦合严格为零）；
  加入 F′=1/2 相消干涉因子；Δ_hf 符号纠正为 +5.936 GHz。
- **v1** 初稿。线宽标定 d、高斯光束链条、角向系数经复核正确并沿用至今。
