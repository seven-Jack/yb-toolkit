# 修订记录

格式：`物理 / 界面 / 性能 / 对账 / 待定`，每条写清**改了什么**与**为什么改**。
半年后回头看，"为什么"通常比"什么"更有价值。

## [0.3.2b] — 2026-08-04 — rabi 椭圆光斑 + η·d 读数

### 界面（按 3b 功能裁决补进 rabi-power 模块）
- **补「有效矩阵元 η·d」读数**（结论条新增一格）：η·d = d_cyc × η，功率标定用。
- **补「椭圆光斑」勾选解绑 wx/wy**：默认圆光斑（wx=wy），勾选后展开独立的 w_x/w_y 输入。
  Store 的 beam.wx / beam.wy 本就是两个字段，物理层（physics.js 的 rabiHz/ramanOmega/
  intensity 全接受独立 wx,wy）从未假设相等 —— 本次只解开了界面的单值绑定。
- **验证 1/(wx·wy) 标度**：wx=100µm/wy=400µm 与 wx=wy=200µm（√(wx·wy)=200µm）
  给出完全相同 Ω_R，确认无单值 w 偷换。

### 对账（3b 功能裁决）
- 旧页「手动 d 输入」「由线宽反推 d_cyc 面板」「对照表双向」三项**接受丢失**：
  手动 d 输入是规则⓪的漏洞（去掉是修正而非功能损失），模块已自动由 Γ 反解；
  对照表双向与核心三向求解重复。
- 四锚点不变（d_red=0.540659 / d_cyc=0.312150 / λ=555.802363 / Ω_R=1.7261），
  默认圆光斑。

## [0.3.2] — 2026-08-04 — 首次真实浏览器实测

> 里程碑：`tests/smoke.js` 的 DOM 全是桩，canvas 渲染、布局、拖拽、fetch 加载数据库
> 从未在真实浏览器里跑过。本次用 Playwright 驱动系统 Chrome 实测，补齐这块空白，
> 并由此揪出几处「只在真实环境出现」的 bug。**没有改任何物理常数 / 公式 / Store 派生量规则。**

### 界面（真实环境暴露的 bug）
- **修复独立页 `tools/raman-qubit.html` 加载即抛异常。** 该页 `pick()` 里
  `document.getElementById('v-d').style.display=''` 但页面没有 `v-d` 元素（字段命名
  不同），且无 null 保护 → 每次加载都抛 `Cannot read properties of null`。属于 README
  待办 #3「画布代码尚未迁移」的连带症状。本页共 **三处** 同类访问（`pick()` 的 style、
  `num('v-d')` 读 value、`calc()` 写 value），逐一加保护；`num()` 助手本身也补了
  null 保护。这是本项目第二次栽在「假设某元素存在但页面没有」上。
- **修复 hfs 模块的异步就绪竞态。** 根因：`cur={el:null,...}` 初始化，要等 `loadDB()`
  的 Promise 兑现后 `syncFromStore()` 才填值；但 `$('exp1').addEventListener('toggle',
  render)` 是同步注册的。`render()` 里 `var d=DB[cur.el]` → `DB[null]` 是 undefined →
  `d.tr` 抛错。两个触发场景：DB 已加载但 cur 未就绪的时间窗内展开折叠区；以及 file://
  下 loadDB() reject、cur 永远为 null 后任何 toggle 都必抛。修复：`render()` 开头加
  `if (!DB || cur.el === null || cur.tr === null) return;`。已加烟雾测试防回归
  （移除该守卫会让测试变红）。排查结论：rabi-power / raman-qubit 无异步初始化，
  无此竞态；全项目该模式就此一处。
- **修默认分割比与紧凑阈值不匹配。** 默认 55% 分割 + 960px 视口下，下窗格净高约 400px，
  已低于 420px 紧凑阈值 —— 开箱即是紧凑模式（折叠区全收起、塞曼表截断），用户一打开
  就遇到。为何调阈值而非分割比：888px 视口下无论分割比怎么设，两个窗格都无法同时超过
  420px（各需 464px、合计 928 > 888），所以 420px 阈值在常见短视口下必然触发其一。
  改法：`COMPACT_PX` 420 → **380**（紧凑模式在净高不足 380px 时才触发），并把默认
  `splitPct` 55 → **50**（更对称）。此时 960px 视口下两窗格各约 400px 净高，均 >380，
  默认不再紧凑。browser.js 加断言：默认加载后两窗格都不应处于紧凑模式。

### 测试（首版 tests/browser.js，34 项）
- 新增 `tests/browser.js`：真实浏览器实测四项要求（联动提示条数值 / J≠0 不出 NaN /
  紧凑模式 / fetch 两路径 + file:// 提示），外加常规项（组合切换、拖拽、复制链接、
  撤销重置、独立页、性能），收集 console error、截图存 `tests/screenshots/`。
- 接入 CI：拆成两个 job —— `vectors`（四套物理/挂载测试）每次 push 都跑；
  `browser`（真实浏览器实测）只在 push 到 main 或 pull_request 时跑，避免每次 dev
  push 都承受 Playwright 浏览器下载与运行成本。
- 新增 `favicon.svg` 并在各页引用，消除控制台里常驻的 favicon 404（非 bug，但会干扰
  以后排错）。

### 数据（任务 2：消灭重复的元素数据库）
- **独立页 `tools/hfs-matrix-element.html` 不再内联 `const DB=[...]`，改为 fetch 同一份
  `data/transitions.json`。** 此前元素数据库有两份（`data/transitions.json` 与独立页内联
  `const DB=[...]`），内容一致但正是本项目反复要消灭的模式（已因"同一份东西存两处"栽过
  三次：d 归一化、Δ_hf 符号、α 硬编码）。现数据唯一来源是 `data/transitions.json`。
- **fetch 降级提示：** file:// 下 fetch 被 CORS 拦截时，显示「请通过 HTTP 打开：
  python3 -m http.server 8000」，与模块版同一套措辞，不再白屏或抛看不懂的异常。
- **异步就绪守卫（同一模式第四处）：** 独立页改异步加载后，`compute()` / `loadDB()` /
  `buildTr()` / `buildIso()` / 自检 `$("run")` 全部加 `if(!ready())` 守卫，避免 DB 未
  就绪时访问 `DB[...]` 抛错。与模块版 hfs 的竞态修复同源。
- **加载态：** 顶部新增 `#dbStatus`，加载中显示「加载元素数据库…」，成功后显示
  「已加载 N 个元素」，失败显示 HTTP 指引。
- **测试：** `tests/bench.py` 新增全库校验（18 条跃迁，lam/ek 一致、dFromGamma 有限为
  正）；`tests/smoke.js` 新增「仓库内无第二份元素数据库（无 const DB=[ 字面量）」检查。

### 对账（测试脚本自身的假象 —— 记下来省下次的坑）
- **移植带计算的功能，验证要到"物理闭合"层，不能只看图画出来。** 3a-2b 移植波片/功率
  拟合时，模块功率扫描幅度比一度输出 993（荒谬）——如果只看"图出来了、没报错"会一路
  蒙混过关。是反推等效束腰 366.3µm ≈ 设定 365µm 这个物理闭合把它逼出来的。此后移植
  带计算的功能一律照此标准：算出一个能交叉验证的物理量（束腰闭合、残差量级等）。
- **独立页 vs 模块的理论值差异是 v-d bug 的延伸后果（3b 薄壳后自动消失）。** 独立页
  `tools/raman-qubit.html` 的拟合理论值（Ath/K1）恒为 0，因为 `S.d = num('v-d')*AU = 0`
  （v-d 元素缺失 → num 返回 0），所有耦合理论都被清零。模块版读 Store 的真实 `V.d`，
  理论值正确（功率幅度比 0.993、束腰闭合）。3b 把独立页改成薄壳后此差异自动消失；
  在此之前，以模块版为准，独立页的拟合理论值不可信（其 θ₀/峰值/RMS 拟合算法本身没问题，
  坏的只是理论对照值）。
- **J≠0 时画布清空是正确行为，断言最初写反、误判为失败。** 无效态下图表被 `prep()`
  清空（0 不透明像素）是设计意图（不显示旧数值），不是空屏 bug。这类"清空 vs 空白"
  的断言方向，以后遇到要先确认哪边才是正确行为。
- **紧凑模式最初用 Yb 测，看不出截断。** Yb 塞曼表本就只有 6 行，30→6 的截断在它身上
  无体现，会"让一个坏掉的功能看起来是好的"。换成 Sr（30 行）才验证到 6 行截断。
  测试用例选数据，要让目标差异足够大。
- **折叠区不会自动重新展开。** `setCompact(false)` 只负责收起、不负责展开，恢复高度后
  `<details>` 需用户手动点开。这是有意设计，不是 bug；断言只应锁行数恢复，不应锁自动展开。

### 待定
- **教训（第二次同类，已写入 README 硬规则⑤）：批量改写源码也会损坏文件。** 3a-2a
  拆分时用 Python 一次删掉波片代码块，误删了 `compute()` 与 `V.ram`，模块语法错误、
  页面直接起不来。与上次 `json.dump` 损坏数据库是同一模式：用脚本大范围改写文件，
  改完才发现坏。此后一律用精确的小步 Edit，每步立即 `node --check` 验证；需要临时
  构造坏数据/坏代码时在内存或临时文件里做，不碰工作区源码。另：阶段性成果（如 3a-2b
  的完整实现）不要只存在 /tmp，应尽快用 `git stash push` 或临时分支落入仓库。
- **教训：测试"坏数据能否被捕获"时不要用 `json.dump` 改写磁盘上的真文件。** 曾为验证
  `validate_transitions()` 能捕获坏数据而临时 `json.dump` 改 `data/transitions.json`，
  意外损坏了文件（一度只剩 `{`）。`json.dump` 还会重排键序、把中文转成 `\uXXXX`、丢缩进
  —— 即使不损坏也产生大量无意义 diff。正确做法：`copy.deepcopy` 读入后**在内存里**构造
  坏数据，把数据对象传给校验函数（`validate_transitions()` 已重构为接受 `db` 参数），
  不碰磁盘。真正改数据文件要走 `git` 或精确的编辑工具。
- **`render()` 竞态修复的「DB 已加载但 cur 未就绪」半段无测试保护。** 烟雾环境里
  `fetch` 被 reject、DB 永远是 null，走不到「DB 已加载但 cur 为 null」的微任务窗口，
  因此烟雾测试只覆盖了「DB 未加载」半段——若有人把守卫退回成 `if (!DB) return`，
  测试仍会绿。唯一能覆盖后半段的是 `tests/browser.js`（真实 HTTP 加载下有这个窗口），
  目前尚未为该窗口写专门断言。记下而非假装覆盖：这是已知的测试边界。
- **默认分割比与紧凑阈值不匹配（本次已修，见 [0.3.2]「界面」）。** 遗留说明：默认 55%
  分割 + 960px 视口下，下窗格净高约 400px，已低于 420px 紧凑阈值 —— 开箱即是紧凑模式。
  修法见正文：降 `COMPACT_PX` 至 380（因为 888px 视口下无论怎么分，两个窗格都无法同时
  超过 420px，阈值是唯一可调的杠杆），并把默认分割改为 50/50。
- 上一版遗留：~~`data/transitions.json` 与 `tools/hfs-matrix-element.html` 内联
  `const DB=[...]` 仍是同一份数据的两份拷贝~~（已由任务 2 消除）。

## [0.3.1] — 2026-08-03 — 自查修复

### 物理
- **修复 J≠0 基态时静默返回 NaN。** 在 hfs 模块选中 Rb 等碱金属（基态 J=1/2）后，
  `d_cyc` 变为 NaN，rabi-power 模块整屏显示 NaN 且无任何提示。
  物理上返回 NaN 是对的 —— `d_cyc = d_red/√(2J′+1)` 的简化只在 J=0 基态成立，
  碱金属基态有两个 F，6j 因子不再被吸收 —— 但界面必须说明原因。
  新增 `derived.dCycValid()`，不适用时读数显示"—"并给出完整解释。
  已加入测试向量防回归。

### 界面
- 补齐提示条 LABEL 表的 12 个缺失路径（`beam.theta_kB_deg`、`limits.*` 等）。
  此前这些量改动时提示条显示原始路径名，违背"必须报出具体量"的设计意图。

### 已知问题（待 Claude Code 处理）
- **元素数据库有两份**：`data/transitions.json` 与 `tools/hfs-matrix-element.html`
  内联的 `const DB=[...]`。目前内容一致，但这正是本项目反复强调要消灭的模式。
  独立页面需改为 fetch 加载同一份 JSON。

## [0.3.0] — 2026-08-03 — 单页外壳 + 模块化 + 全局 Store

### 架构
- **从多页站点改为单页外壳 + 上下分栏。** 两个窗格可各选一个模块同时显示，
  中间分隔条可拖拽（双击复位）。窗格净高低于 420 px 时模块自动进入紧凑模式：
  收起折叠区、缩小图高、塞曼分量表只显示前 6 行。
- 新增 `shell/store.js`：全局状态。**硬规则是只存输入、不存派生量** ——
  存 `transition`（元素/同位素/能级/Γ），`d`、`λ`、`CG`、`Ω` 全部现算。
  缓存派生量会产生"改了同位素但 d 没跟着变"这类静默错误。
- 新增 `shell/registry.js`：模块注册与挂载。每个挂载实例分配唯一 id 前缀 ——
  rabi-power 与 raman-qubit 都用了 `v-p`/`v-d`/`v-f`，且同一模块可能同时挂在
  两个窗格，不隔离必然冲突。
- 新增 `shell/panes.js`：分栏、拖拽、紧凑模式、URL 编码（含窗格布局与分割比例）。
- 三个工具改写为 `modules/*.js`。模块只与 Store 对话，彼此不直接依赖 ——
  加第四、第五个模块时不会退化成一张依赖网。

### 联动
- 模块间自动联动，**外部改动会弹出提示条，并报出具体哪个量从多少变到多少**。
  只说"跃迁已改变"没有意义 —— 使用者需要能立刻判断影响。
  典型场景：hfs 模块里选中 Yb 556，其数据库 Γ = 182 kHz 会覆盖默认的 183 kHz，
  拉曼模块的 Ω_R 随之从 1.7261 降到 1.7166 MHz（−0.55%）。提示条会写明这一点，
  并提供撤销。
- `reads` / `writes` 声明区分"自己改的"与"别人改的"，前者不弹提示 ——
  否则拖个滑块就会弹一条。

### 性能
- 单位解析移出绘图热循环。此前 `uP()` / `uF()` / `uW()` 在每帧的曲线循环里
  被调用上千次，每次都做一遍查表。现改为每次重绘开始时解析一次并缓存，
  与 v7 的主题色缓存同理。`YBU.resolve` 对非法单位退回默认而非抛异常
  （畸形 URL 不应让整个模块崩掉），并对同一非法值只警告一次。

### 测试
- 新增 `tests/smoke.js`：无浏览器的挂载烟雾测试，16 项。校验三个模块能注册、
  能挂载、同一模块挂两次 id 前缀不冲突、Store 联动生效、派生量确实重算、
  撤销能恢复。**并锁定四个数值锚点**（d_red、d_cyc、λ、Ω_R），
  确保改架构不改变物理。已接入 CI。
- 物理向量仍为 35/35，JS 与 Python 双通道全通过。

### 保留的重复
- `tools/*.html` 三个独立页面保留，作为完整功能版本，主页面窗格标题栏有
  "完整版 ↗" 链接。模块目前只覆盖核心视图，尚未包含三维曲面、波片扫描拟合、
  42 组合自检等。**物理不会分叉**（两者共用 `shared/`），暂时重复的只有界面代码。
  收敛计划见 README「待办」。

## [0.2.0] — 2026-08-03 — 并入超精细矩阵元计算器

### 物理修正
- **波长改用 NIST 能级导出。** `λ_556` 由硬编码 556.0 nm 改为
  `10⁷/E_k`（E_k = 17992.007 cm⁻¹）= **555.802363 nm**。这从根上避免
  空气/真空波长混用。原值偏高 0.036%，使 d 偏高 0.053%。
  值得记的是：notebook cell 3 由 rydcalc 独立算出的正是同一波长，
  证据一直都在，只是此前没被采纳。
- **线宽保留两个值。** 默认 Γ/2π = 183 kHz（notebook 标定所用），
  备选 182 kHz（hfs 计算器所用），并记录文献分散 182–184 kHz。
  d ∝ √Γ，两者差 0.27%，均在实验不确定度内。
- 由上述两项，`d_cyc` 0.312316 → **0.312150**，`d_red` 0.540947 → **0.540659**。
  下游 Rabi 频率随之改变约 0.05%。

### 架构
- 新增 `shared/wigner.js`：Wigner 3j/6j（Racah 公式 + 对数阶乘），
  与 sympy 吻合到 1e-16，三角不等式不满足时返回 0 而非抛异常。
- **CG 表由硬编码改为 Wigner 计算。** 计算值与原硬编码值逐位相同，
  包括 F′=1/2 分支的负号 —— 该负号此前仅由推导得出，现由独立路径确认，
  它正是拉曼相消干涉因子 `Δ_hf/(Δ+Δ_hf)` 的来源。
- 新增工具 `tools/hfs-matrix-element.html`，删除其页面内重复的 Wigner
  实现改为引用共享模块（50.4 → 42.5 KB）。
- 测试向量 21 → 35 项，新增 `wigner` 与 `dipole_convention` 两组。
  其中「d_red = √3·d_cyc 两条路径一致」直接锁死了归一化约定。

### 采纳的纪律
- 数据库不存任何文献 d 值，一律由 Γ 反解。该原则来自 hfs 计算器
  v2.1 的教训（约定问题上曾两次判断失误），已写入 README 置顶规则。

### 备注
- 编写本次测试向量时，「3j 半整数」一项的期望值最初凭记忆填写并出错，
  由 sympy 核实后修正为 √30/15。这恰好说明向量的期望值必须来自
  权威计算或文献原文，不能来自记忆或当前代码的输出。

## [0.1.0] — 2026-08-03 — 框架搭建

### 架构
- 从两个独立 HTML 文件整合为多页站点，共享模块化。不引入构建工具：
  双击即可打开、断网可用、任何人能改一行 HTML。用 `<script src>` 而非
  ES module，因为后者在 `file://` 下会被 CORS 拦截。
- `shared/constants.js` 成为物理常数唯一来源，每项带 `src` 出处。
  **这是本次最重要的改动** —— 历史上 d 的两种归一化、Δ_hf 符号、
  α 被硬编码为 45°，全部源于同一常数在不同文件各写一份。
- `shared/physics.js` 成为公式唯一来源；`shared/plot.js` 沉淀 v7 的
  性能修复（主题色缓存、画布尺寸缓存、离屏复用、调度器）。
- `tests/vectors.json` 由 `bench.js` 与 `bench.py` 共同读取，
  两套实现必须同时通过；`crosscheck.js` 校验两侧常数逐位一致。

### 新增
- URL 状态编码：任一次计算都能变成可分享、可复现、可存档的链接。
- 版本戳：页面角落与 CSV 注释均带 `版本 · 日期 · commit`，
  排错时不必再猜对方是不是缓存了旧版。
- 控制台 API `window.YB`，按 F12 即可做零散计算。
- `data/` 目录纳入版本管理，标定值与文献值分离且可追溯。

### 已知待办
- `tools/raman-qubit.html` 的画布代码尚未迁移到 `shared/plot.js`，
  因此仍存在 v7 之前的布局抖动问题（连续拖动滑块会卡）。
- 两个工具的单位下拉尚未统一走 `shared/units.js` 的 `bind()`。
- `data/calibrations.json` 全为 null，待实验组填入。
  **在偏振与几何标定完成前，拉曼工具的绝对幅度不应作定量预测。**

---

以下为整合前各工具的历史，完整内容见对应页面内的「修订记录」区块。

## rabi-power（原 yb_rabi_power v5–v7）
- **v7** 消除输入卡顿。根因是布局抖动而非计算量：每帧约 100 次
  `getComputedStyle` 与 3 次 `clientWidth` 强制同步重排，中间又夹着
  `innerHTML` 重建。改为缓存 + 批量写入后降至 2.1 ms/次按键。
- **v6** 坐标裁剪到实验可达范围；单位自选；公式改用可分离形式
  `f_R = C√P/√(w_x w_y)`（数值逐位一致，相对偏差 < 3e-16）。
- **v5** 精简为纯矩阵元驱动，逐点复现 notebook 输出。

## raman-qubit（原 yb171_raman_calculator v1–v4）
- **v4** 补入微分光频移 `Δ_LS = δ_Z(8Ω_σ²+Ω_π²)/(4Δ²)`，工作点给
  55.6 kHz（论文 55.5 kHz）。此前只报散射误差，总门误差低估约 3.7 倍。
  修正 S₃ 模式下 α 被硬编码为 45° 的缺陷。
- **v3** 几何因子改写为 `|S₃|·sinΘ_kB`；新增 λ/4 波片模型 `S₃ = sin2(θ−θ₀)`。
- **v2** φ 改为 φ_HV 且 cos → sin（v1 默认工作点实际耦合严格为零）；
  加入 F′=1/2 相消干涉因子；Δ_hf 符号纠正为 +5.936 GHz。
- **v1** 初稿。线宽标定 d、高斯光束链条、角向系数经复核正确并沿用至今。
